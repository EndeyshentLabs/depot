include "../std/mem.dpt"

link "c"

extern proc malloc int64 -- ptr ;
extern proc free ptr -- ;
extern proc putchar int64 -- ;
extern proc memcpy ptr ptr int64 -- ptr ;

extern proc puts ptr -- ;
proc c/puts int64 ptr -- { >cstr puts }
