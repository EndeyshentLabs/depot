include "../std/c.dpt"

extern proc printf ptr int64 -- ; // NOTE: add `poly` type
proc printn int64 -- { "%d" >cstr swap printf }

proc main -- int64 {
    1 while dup 100 <= {
        dup 15 mod 0 = if
            "FizzBuzz" c/puts
        else dup 3 mod 0 = elif
            "Fizz" c/puts
        else dup 5 mod 0 = elif
            "Buzz" c/puts
        else
            dup printn 10 putchar
        then

        1 +
    }
    drop
    0
}
